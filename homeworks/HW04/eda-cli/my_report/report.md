# Анализ пользователей

## Настройки отчёта

- Исходный файл: `example.csv`
- top_k_categories: `10`
- min_missing_share: `5.00%`
- high_cardinality_threshold: `50%`
- zero_share_threshold: `90%`
- max_hist_columns: `10`

## Общая информация

Строк: **36**, столбцов: **14**

## Качество данных (эвристики)

- Оценка качества: **0.59**
- Макс. доля пропусков по колонке: **5.56%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**

### Новые эвристики качества

- Константные колонки: **False** (кол-во: 0)
- Высокая кардинальность категориальных признаков: **False** (кол-во: 0)
- Дубликаты в ID-колонках: **True** (кол-во: 1)
  - `user_id`: уникальных 35 из 36 (2.8% дубликатов)
- Много нулей в числовых колонках: **False** (кол-во: 0)
- Проблемные колонки с пропусками (> 5%): **2**
  - Колонки: city, avg_session_duration_min

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Топ-10 значений для категориальных признаков:

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

Построены гистограммы для первых 10 числовых колонок.
См. файлы `hist_*.png`.
